services:
  centrifugo:
    image: centrifugo/centrifugo:v5
    container_name: rfq-centrifugo
    command: centrifugo -c /centrifugo/config.json
    ports:
      - "8000:8000"
    volumes:
      - ./json/config.json:/centrifugo/config.json:ro
    restart: unless-stopped